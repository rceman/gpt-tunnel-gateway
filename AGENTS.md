# Agent contract

Use the canonical `rceman/gpt-review-planner` workflow pinned in `.gpt-workflow.lock`.
Read work with `gpt-tunnel task read <task-id>`. Airelay is only a bounded
control channel; completion requires a committed hub result and evidence.
