# CLI and MCP contract

The CLI names are `gpt-tunnel`, `gpt-tunnelctl`, and `gpt-tunnel-gatewayd`.
Operations are typed project/plan/ADR/task/run commands. MCP mirrors these
operations and returns object-shaped `structuredContent`; arbitrary execution,
Git, and filesystem tools are forbidden.
