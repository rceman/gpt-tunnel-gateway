# Migration

This project is additive and does not stop, restart, reconfigure, or deploy over
the running `ai-workspace` daemon or tunnel. Migration of existing hub state and
controller ownership requires a separate owner-approved change.
