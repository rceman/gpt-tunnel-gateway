# ADR-0001: Use Go for the gateway runtime

Status: accepted.

Go provides static host-native binaries, standard-library HTTP/process/Git orchestration, and direct alignment with `gpt-github-gateway`. Rust `workspace-agentd` is not carried forward.
